# Production Environment Variables Template
# Copy this to .env in production and fill in actual values

# Backend Environment Variables
NODE_ENV=production
PORT=10000

# MongoDB Atlas Connection (Get from MongoDB Atlas Dashboard)
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/sports_vision_trainer?retryWrites=true&w=majority

# JWT Secrets (Generate strong random strings - 32+ characters)
# You can generate these using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-change-this
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-long-change-this

# Frontend URL (Your Vercel deployment URL)
FRONTEND_URL=https://your-app-name.vercel.app

# Frontend Environment Variables
NEXT_PUBLIC_API_URL=https://your-backend-name.onrender.com

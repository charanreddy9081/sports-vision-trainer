# Root Environment Variables Example
# This file shows all environment variables needed for the project

# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================
# Copy these to backend/.env for local development
# or set them in your hosting platform for production

NODE_ENV=development
PORT=5000

# Database Connection (MongoDB)
# For local: Use local MongoDB or MongoDB Atlas
# For production: Use MongoDB Atlas
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/sports_vision_trainer?retryWrites=true&w=majority

# JWT Secrets (Generate strong random strings)
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-long

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# Copy these to frontend/.env.local for local development
# or set them in Vercel for production

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:5000
